{% extends 'base.html.twig' %}

{% block title %}Liste des articles{% endblock %}

{% block body %}
	<article>
		<h3>{{ article.title }}</h3>
		publié le {{ article.datepubli|date('d/m/Y') }} par {{ article.author }}
		<p>{{ article.content }}</p>
	</article>
{% endblock %}
